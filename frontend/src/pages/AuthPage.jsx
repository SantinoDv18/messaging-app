// Importa el componente principal de autenticación (login/registro)
import AuthForm from "../components/forms/AuthForm";

// Componente de página (vista completa)
// Representa la pantalla de autenticación de la aplicación
export default function AuthPage() {

  // Renderiza el formulario de autenticación
  return <AuthForm />;
}